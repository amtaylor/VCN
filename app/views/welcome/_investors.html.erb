<div class="investors" id="investors">

	<div class="second">
		<h3>Know your VC nemeses:</h3>

		<table class="table table-hover">
			<thead>
				<tr>
					<th>VC Nemeses</th>
					<th>Competitors they’ve invested in</th>
				</tr>
			</thead>
			<tbody>

				<% if @investors.size > 0 %>
					<% @investors.each do |investor| %>
						<tr>
							<td> <%= investor.name %> </td>
							<td>
								<% Investor.companies_for_investor(@user, investor).each do |vc_company| %>

									<% if vc_company.exited? %>
										<span class="text-muted">
											<%=  vc_company.name %>
											(exited)
											<%= "," unless vc_company == Investor.companies_for_investor(@user,investor).last %>

										</span>
									<% else %>
										<%=  vc_company.name %><%= "," unless vc_company == Investor.companies_for_investor(@user, investor).last %>
									<% end %>
								<% end %>

							</td>
						</tr>
					<% end %>
				<% else %>

						<tr>
							<td><span class="text-muted">None</span></td>
							<td><span class="text-muted">None</span></td>
						</tr>

				<% end %>

				<!-- original sample table
				<tr>
					<td>Andressen Horowitz</td>
					<td>Square, Twitter, <span class="text-muted"> Google (exited)</span> </td>
				</tr>
				<tr>
					<td>KPCB</td>
					<td>Kabam, <span class="text-muted"> Zynga (exited) </span>, Rocket Fuel</td>
				</tr>
				<tr>
					<td>Sequoia</td>
					<td>Square, Twitter</td>
					</tr>
				<tr>
					<td>Keith Rabois</td>
					<td>Square</td>
				</tr>
				-->

			</tbody>
		</table>

	</div>

</div>
